let y;
const x = '' + prompt('Введите число', -1);

if (x < 0 && x > -2) {
 y = 0;
} else {
 y = 2;
}

if (y === 0) {
    alert('Делить на ноль нельзя')
}

// решение
let y;
const x =  + prompt('Введите число', -1) ;
y = (x >= 0 || x <= -2 ) ? (alert("Делить на ноль нельзя"), 0) : 2;